Jag använder PostgresPresql för att starta lokalt, byggnaden ska finnas i migration för min codefirst setup. AI har används för frontend bara, den genererade stor mängd av koden i react med hjälp av ChatGPT.

För backend så använder jag CRUD uppbyggnader på alla tables så man ska enkelt kunna sätta requierments och lättare felsöka även komma åt endpoints. Frontend är ett simpelt UI för att testa det olika endpoints tillsammans. Intergartionstestet är för Deltagare och var skapat för hand.

Uppbyggnad av databasen är i Databas.pdf, Och en större beskrivning på tänket bakom strukturen av den finns i Modellering och Normalisering.

